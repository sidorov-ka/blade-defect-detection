# MLflow Configuration (optional)
# MLflow server should be running on this URI for experiment tracking
# If not set or server unavailable, training continues without MLflow logging
MLFLOW_TRACKING_URI=http://127.0.0.1:8080

# Data Directory (optional, defaults to 'data')
# Can be overridden via Hydra config: configs/data/dataset.yaml
DATA_DIR=data

